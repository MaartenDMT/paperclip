auto-install-peers=true
# Cross-drive symlinks from A:\pnpm-store into the project node_modules\.pnpm
# fail silently on this Windows host, leaving node_modules empty after install
# (confirmed: 44m install completes, postinstall scripts run, but virtual store
# never materialises). The hoisted linker copies files which works reliably.
node-linker=hoisted
